% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_sheets.R
\name{build_pheno_from_idat_dir}
\alias{build_pheno_from_idat_dir}
\title{Build a pheno data.frame for an IDAT directory tree}
\usage{
build_pheno_from_idat_dir(root_dir, verbose = TRUE)
}
\arguments{
\item{root_dir}{Directory to search.}

\item{verbose}{Print discovery progress.}
}
\value{
List with:
\itemize{
\item \code{pheno}         Combined data.frame keyed by Sentrix basename
(rownames = colnames(betas) after IDAT processing)
\item \code{idat_basenames} Vector of Sentrix keys found
\item \code{n_synthesized}  How many rows were synthesized
\item \code{synthesized_keys} Which Sentrix keys were synthesized
\item \code{sheets_used}    Vector of sample-sheet paths consumed
}
}
\description{
Looks under \code{root_dir} (and all subdirectories) for IDAT files and sample
sheets, matches them by Sentrix key, fills in synthesized rows for any
IDATs without a matching sheet, and returns a pheno data.frame keyed by
Sentrix basename (suitable for direct use as \verb{clocker(... pheno = ...)}).
}
\details{
Discovery rules:
\itemize{
\item IDATs found recursively from \code{root_dir}.
\item Sample sheets found recursively, identified by having a header row
containing any of: Sample_Name, Sentrix_ID, Sentrix_Position, Basename.
\item When multiple sheets cover the same Sentrix key, the \strong{deeper}
subdirectory sheet wins (more specific). Among same-depth sheets,
the most recently modified wins. Conflicts emit a single warning.
\item IDATs with no matching sheet row get a synthesized row with
Age = NA, Female = NA, and a warning listing the affected basenames.
}
}
\keyword{internal}
